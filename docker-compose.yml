services:
  dashboard:
    build: .
    container_name: executive-dashboard
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
    environment:
      - APP_DATA_DIR=/app/data
    restart: unless-stopped
